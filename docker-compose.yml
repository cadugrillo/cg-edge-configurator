version: "3.8"

services:
  cg-edge-conf-api:
    image: cadugrillo/cg-edge-conf-api:1.0
    container_name: cg-edge-conf-api
    restart: always
    volumes:
      - type: bind
        source: /home/edge/apps
        target: /apps
    networks:
      - cg-edge-conf

  cg-edge-conf:
    image: cadugrillo/cg-edge-conf:1.0
    container_name: cg-edge-conf
    restart: always
    ports:
      - 80:80
    networks:
      - cg-edge-conf

networks:
   cg-edge-conf:
    external: true
  
   

